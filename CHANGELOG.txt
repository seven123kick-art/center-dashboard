Version6 配送分析 Phase D-3: 便別採算タブ UI完成
（2026-08-15）

【目的】
Phase D-1（グローバルCSS汚染解消）を土台に、便別採算タブ自体を
Version6完成5画面（月次収支/科目分析/推移分析/予実差異分析/
着地予測）と統一感のあるUIへ仕上げた。他5タブ（作業者別/作業内容/
商品カテゴリ/エリア別/キャパ分析）は今回一切変更していない。

【変更ファイル】
- center.html
- src/field/route_analysis.js
- assets/css/features/route-analysis.css

============================================================
①変更ファイル
============================================================
center.html / src/field/route_analysis.js /
assets/css/features/route-analysis.css の3ファイルのみ。

============================================================
②独自ヘッダー変更内容
============================================================
「DELIVERY PROFITABILITY」（.saas-eyebrow）をHTML・CSSの両方から
削除した。「便別採算」（.saas-page-title）「売上・傭車支払・
一次利益を便単位で把握し、原票まで掘り下げます。」（.saas-page-sub）
はテキストとして維持し、.saas-page-titleのfont-sizeを25px→16px
（既存完成画面のセクションタイトル程度の自然な強さ）へ調整した。
letter-spacing/marginも合わせて調整。HTML構造・要素そのもの
（<h1>/<p>）は変更していない。

============================================================
③フィルター変更内容
============================================================
機能・ID・onchange/oninputイベント・選択値保持ロジックは一切
変更していない。見た目のみ、.saas-toolbarのpadding（13px 15px→
9px 14px）・box-shadow（独自値→var(--shadow)）・border-radius
（14px固定→var(--radius)）を、着地予測/科目分析で確立した基準値
（padding:9px 14px、margin-bottom:14px）へ統一した。
select/inputのmin-height（36px）は元々同じ値だったため変更なし。
対象年月・所属会社・運行区分・検索・一覧/カード切替の各要素は
IDも構造も一切変更していない。

============================================================
④KPI変更内容
============================================================
項目（対象便数/売上/傭車支払/一次利益）はそのまま維持。
下部アクセントバー装飾（accent-navy/accent-green/accent-amber、
および.saas-kpi-grid .kpi-card::after装飾）を、CSS定義ごと削除
した。route_analysis.js側でもkpi-card生成時にaccent-*クラスの
付与自体を削除した（4箇所、数値計算・fmt()呼出・変数参照は
一切変更していない）。center.html側もKPIエリアのラッパーから
saas-kpi-gridクラスを削除した。
一次利益カードの正負色分け（profit-positive/profit-negative、
totalMargin>=0判定）は業務上意味のある表示のため一切変更して
いない。

============================================================
⑤警告変更内容
============================================================
変更なし。既に既存のmsg/msg-warn/msg-info共通クラスを使用して
おり（route_analysis.js内のdiagnostic生成部分）、追加の統一作業は
不要と判断した。文言生成・不足判定ロジックにも一切触れていない。

============================================================
⑥一覧/カード表示変更内容
============================================================
機能・列・データ・イベント（openDetail呼出、setView呼出等）は
一切変更していない。テーブルは既存通りclass="tbl saas-table"の
併用形式を維持（Phase D-1で.tbl自体は既に共通クラスへ復元済み
のため追加変更不要）。カード表示（.route-profit-card等）は白背景・
共通border/box-shadowを既に使用しており、Phase D-1で:root
トークンが正しい値へ復元されたことで自動的に統一トーンとなって
いるため、今回追加のCSS変更は行っていない。

============================================================
⑦モーダルへの影響
============================================================
機能・データ・イベント（openDetail/closeDetail）は一切変更して
いない。.route-detail-panelのborder-radius（18px固定）を、
既存トークンvar(--radius-lg,18px)へ置き換えた（視覚的な値は
完全に同一、保守性のためのトークン化のみ）。他のモーダル内定義
（.route-detail-kpi等）は明確な旧UI逸脱と判断できなかったため、
今回は変更していない。

============================================================
⑧PDF/CSV取込への影響
============================================================
なし。ボタンのonclick（NAV.go('import')/NAV.go('csv-import')）は
一切変更していない。クラス名をsaas-btn-secondary→単純なbtnへ
整理した（見た目のみ、他画面で使用実績のない未使用クラスの
削除であることを事前確認済み）。PDF/CSV取込処理そのもの
（parsePdf/parsePageText/importFiles）は文字単位で完全一致する
ことを確認した。

============================================================
⑨業務ロジック無変更確認
============================================================
marginRate / parsePdf / parsePageText / importFiles / joinedRows
の5関数をPythonでバイト単位比較し、完全一致（変更前後で1文字も
違わない）ことを確認した。LEDGER / STATE / Repository / CLOUD /
SYNC_COORDINATORへの参照件数も変更前後で一致（11件→11件）する
ことを確認した。

============================================================
⑩他5タブ無変更確認
============================================================
field_worker.js / field_content.js / field_product.js /
field_area.js / capacity.jsは今回一切編集していない
（ハッシュ確認済み）。Phase D-2で発見したaccent-*スコープ問題
（field_worker.js/field_product.jsのアクセントバーが現在表示
されない可能性がある件）は、今回意図的に修正していない
（次Phaseの課題として引き継ぐ）。

============================================================
⑪CSSグローバル汚染再発なし確認
============================================================
route-analysis.cssに:root / body{} / .card{} / .card-header /
.card-body / .btn{} / select / input / .tbl th / .tbl td /
.kpi-card{} / .kpi-label / .kpi-value / #content{}のグローバル
再定義が存在しないことを正規表現で確認した（該当0件）。
全ての配送分析固有セレクタは引き続き#view-route-analysis配下へ
スコープされている。

============================================================
⑫JS/CSS構文確認
============================================================
全JS構文チェック：エラー0件
route-analysis.css：中括弧の対応数が一致（{ =55 } =55、構文
エラーなし）

============================================================
⑬残課題
============================================================
・Phase D-2で発見したfield_worker.js/field_product.jsの
  accent-*スコープ問題は未対応（次Phaseの課題）
・実ブラウザでの最終目視確認は開発環境の制約上未実施
・便別採算のExcel出力は今回追加していない（別Phase候補として
  引き続き検討可能）

============================================================
⑭最終判定
============================================================
PASS
