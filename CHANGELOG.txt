M2-8A (2026-08-25) 全SOURCE取込モーダル・保存前検証統一
- 最新全コードZIP center-dashboard-main(20260825-122738).zip を正本に監査して修正。
- データ管理8資料の取込を中央モーダル（読込/検証/保存/成功/失敗）へ統一。
- SKDL0001は全選択ファイルの内部年月を先に検証し、1件でも不一致なら保存0件。
- SKDL0002/0003はACCOUNTING_SOURCE_RECORDのaccounting_dateから内部年月を検証し、画面対象年月不一致を保存前に拒否。
- SKDL0003 Legacy STATE更新前にも同じ内部年月検証を実施。
- DELIVERY_LIST PDF / ROUTE_PAYMENT Excelは内部配達日の月と画面対象年月を保存前に照合。不一致・複数月はSTATE/Normalized Source保存前に拒否。
- 年度予算は既存のPDF内部年度チェックを維持し、結果を中央モーダルへ集約。
- 初期ファイルまとめて選択の解析中/解析完了も中央モーダル表示。
- 初期履歴一括登録は全候補を保存前にpreflightし、年月/センター/既存CURRENT等に1件でも問題があれば保存0件で中止。
- 右下toastに依存せず、データ管理ハブ経由の結果確認を中央モーダルへ統一。
- 既存直接取込画面の互換経路は削除していない。
- GitHub直接書込みなし。

削除候補メモ:
- /app.js : 現行 center.html は /src/app.js を読み込む。最終削除フェーズで全参照再確認後に削除可否確定。

M2-7A (2026-08-25) Canonical荷主分析の会計SOURCE統一
- 荷主分析Canonicalの売上正本を SHIPPER_AREA + WORKER_SALES から PL_ACTUAL(CONFIRMED) / ACCOUNTING_FACT へ変更。
- Legacy確定仕様と同じく「収支科目名に収入を含む行」の金額を合算。
- 荷主基本コードは既存Legacyと同じ正規化後の左4桁で荷主統合、全桁で契約別集計。
- 原票番号ユニークで件数を集計。荷主コードなしの収入は「その他収入（荷主未設定）」として売上へ含め、件数・単価・契約別から除外。
- PL_ACTUAL Normalized Sourceが既に保持する荷主名・契約名・照合コードをACCOUNTING_FACTへ透過。
- SHIPPER_AREAは商品・配送エリア・照合SOURCEとして維持し、荷主会計売上の正本には使用しない。
- 荷主画面のCanonical表示文言をPL_ACTUAL確定CURRENT・左4桁仕様へ更新。
- 保存処理・Normalized Source登録処理・既存Legacy集計ロジックは変更なし。
- 2026-08-24 09:47:35取得の全コードZIPを正本として修正。GitHub直接書込みなし。

M2-6E (2026-08-24) データ取込結果通知の中央一本化
- 全コード確認により、右下青通知の発生元は src/field/field_core.js の共通 msg() → UI.toast と特定。
- データ取込ハブの中央モーダル経由中だけ toast を抑止し、中央の処理中→成功/失敗→確認へ一本化。
- 従来画面から直接行う取込や、その他の msg()/toast は従来どおり維持。
- 保存処理、年月ガード、二重取込防止は変更なし。
M2-6D (2026-08-24) データ取込結果の中央完結
- M2-6Cの中央進捗モーダルを、処理完了後も同じ位置で成功/失敗結果へ切替。
- 成功時は緑の完了表示、失敗時は赤のエラー表示とし「確認」で閉じる。
- この経路の完了/失敗を右下toastへ移さず、開始→処理→結果確認の視線を中央で統一。
- M2-6Bの年月ガード、M2-6Cの二重取込防止を維持。
M2-6C (2026-08-24) データ取込進捗表示
- WORKER_SALES / SHIPPER_AREAのファイル選択直後から中央に「データを読み込んでいます」を表示。
- 資料名・対象年月・処理段階を表示し、処理中の二重取込を防止。
- 完了時は既存toastとカード更新を維持。失敗時も進捗画面を必ず解除。
- M2-6Bの年月同期・内部日付保存前ガードを維持。
M2-6B (2026-08-24) 取込年月のSingle Source of Truth化・誤月保存防止
- データ取込ハブの対象年月と、従来作業者/商品CSV取込の年月セレクタ同期で月を2桁（04等）に統一。
  従来は「4」をselectへ代入しており、option値「04」と一致せず、最新月等へ戻る余地があった。
- データ取込ハブからWORKER_SALES / SHIPPER_AREAを選ぶ場合、従来hidden inputのonchangeに依存せず、
  handleFilesForYM(files, 対象年月) を明示呼出しするよう変更。
- 作業者別CSV / 商品・住所CSVは保存前に正規化レコードの内部配達日から実年月を再確認。
  画面対象年月と一致しないファイルはSTATE・Cloud・Normalized Sourceへ保存する前に停止し、実年月を表示。
- ファイル名は年月の正本にしない。内部日付と画面対象年月の一致を保存条件とする。
- 初期履歴の一括自動判別は複数年月を扱う用途のため、従来どおり内部日付で月別に診断する。
- ROUTE_PAYMENTは既存実装が配達日から年月を算出して月別保存しているため変更なし。
- GitHubへの直接書込みなし。2026-08-24 01:07取得の全コードZIPを正本として修正。

M2-6A (2026-08-22) 初期履歴の安全な実登録
- M2-5P/Qの自動判別・登録前プレビューから、利用者が明示操作した場合だけ初期履歴をNormalized Source Repositoryへ一括登録できる処理を追加。
- 一括登録対象は PL_CONFIRMED / WORKER_SALES / SHIPPER_AREA / ROUTE_PAYMENT の4SOURCEに限定。SKDL0001、年度予算、未確定SOURCE等は対象外。
- 登録候補は対象年月1件・HIGH信頼度・センター確定・選択中センター一致・同一投入内重複なし・既存CURRENTなしを必須条件とする。
- 実行直前に各SOURCEのCURRENTを再確認し、既存CURRENTが存在する場合は上書きせずスキップする。
- WORKER_SALES / SHIPPER_AREA / ROUTE_PAYMENT は既存 SOURCE_NORMALIZER と Repository.NormalizedSource.saveBatch() を再利用。
- PL_CONFIRMED は既存 ACCOUNTING_IMPORT_BRIDGE の normalizeCsvText()/persistRecords() を再利用し、CONFIRMED immutableルールを維持。
- 正規化後にもdocument_type・year_month・内部日付の整合を再検証してから保存する。
- 保存成功月は既存 CANONICAL_MATERIALIZER で再構築。再構築失敗はSOURCE保存を巻き戻さず、結果画面へ明示する。
- 保存中は normalized-source-updated によるデータ取込画面の再描画を抑止し、全件終了後に状態を再読込する。
- 保存結果は 保存/スキップ/失敗 を件数表示し、失敗・スキップ理由を詳細表示する。
- 既存STATEへの初期履歴書込みや既存CURRENTの自動改訂は行わない。
- 基準コード: GitHub main commit 45ddc18519a5d19dfec854e0d0331bdfd1896687。M2-5Rはcenter.html/src/app.js/CHANGELOGのみ変更で、data_import_hub.jsはM2-5Qから変更なしをGitHub compareで確認。

M2-5R (2026-08-22) Repository起動依存の明示化
- center.html の Repository Registry 読込を src/app.js より前へ移動。
- registry.js は getter 方式で個別Repositoryの読込順に依存しない既存設計のため、その設計どおり起動前に名前空間だけを確実に公開する構成へ変更。
- src/app.js のBoot開始時に window.Repository と Repository.Storage.load の存在を明示確認。
- rawな「Repository is not defined」ReferenceErrorではなく、依存ファイルが読み込めない場合は確認対象を明示した起動エラーに変更。
- Cloud同期、Readiness Gate、STATE、データ取込・保存ロジックには変更なし。
- M2-5Qのデータ取込UI可読性改善は維持。

M2-5Q (2026-08-22) データ取込 診断UI可読性改善
- 初期データ自動判別の初期表示を、全ファイル技術情報の羅列から運用サマリー中心へ再構成。
- 月別資料セットをカード表示化し、正常月と不足・重複・センター要確認月を視覚的に分離。
- 登録前プレビューは要確認/除外を優先表示し、登録候補は折りたたみ表示へ変更。
- 全ファイルの信頼度・状態・判定根拠は「全ファイルの技術判定」に折りたたみ、必要時のみ確認可能。
- 長いファイル名・SOURCE・判定根拠を折り返して文字切れを抑止。
- M2-5PまでのSOURCE判定・期間判定・センター補完・登録候補判定ロジックは変更なし。
- CURRENT / STATE / Cloudへの保存処理は追加していない。

M2-5P (2026-08-21) 初期履歴 登録前プレビュー
- M2-5Oの診断結果から「登録候補」と「要確認/除外」を自動分離する登録前プレビューを追加。
- 登録候補は SOURCE確定・内部期間1件（または年度確定）・センター確定・HIGH信頼度・同一投入内重複なし・既存CURRENTなしを全て満たすものに限定。
- UNKNOWN / ERROR / PENDING_PARSER / PL_DAILY_ACTUAL、複数月、期間不明、センター未確定、重複候補、既存CURRENT、HIGH未満は要確認/除外として理由を明示。
- M2-5Oの安全なセンター補完および月別4SOURCE診断を維持。
- 本Phaseは登録可否のPREVIEWのみ。保存ボタン・CURRENT / STATE / Cloud書込みは追加していない。

M2-5O (2026-08-21) センター安全補完＋月別資料セット診断
- 同一投入バッチ・同一内部期間にHIGH信頼度の明示センターが1種類だけ存在する場合に限り、センター情報を持たない既知SOURCEへセンターを補完。
- ファイル名や選択中センターだけを根拠にした補完は禁止。複数センターが混在する期間は補完しない。
- 補完された行は画面上で「（補完）」と表示し、判定根拠にも補完元ルールを明記。
- 初期履歴用4SOURCE（PL_CONFIRMED / WORKER_SALES / SHIPPER_AREA / ROUTE_PAYMENT）について、月別の充足数・不足SOURCE・重複候補を診断する表を追加。
- M2-5NのSOURCE別正式日付列判定を維持。
- 本Phaseも診断専用。CURRENT / STATE / Cloudへの保存処理は追加していない。

M2-5N (2026-08-21) SOURCE別期間判定の厳格化
- 初期データ自動判別の年月抽出を、ファイル全体の「日付らしい数字」走査からSOURCE別の正式日付列判定へ変更。
- WORKER_SALESは「配達完了日」、SHIPPER_AREAは「配達完了日」、PL_CONFIRMEDは「計上日」、ROUTE_PAYMENTは「配達日」だけを対象年月の根拠に使用。
- 2000年/2036年/2080年など、住所・伝票番号・その他項目を年月として誤認する問題を根本修正。
- CSV解析は自動判別専用のRFC4180相当行パーサーを内包し、既存CSV.parseの返却形状に依存しないよう変更。
- センター判定もSOURCE別の明示列だけを使用。WORKER_SALESのように資料内にセンター情報がない場合は推測せず「センター要確認」とする。
- ファイル名の年月は登録判定には使用せず、内部日付との一致/不一致を監査表示する補助情報だけに限定。
- 内部日付列が取得できない既知SOURCEは「期間不明」として要確認扱いにし、勝手に登録可能判定しない。
- 実データ確認: WORKER_SALES 2025/04→202504のみ、SHIPPER_AREA 2024/04→202404のみ、PL_CONFIRMED 2025/06→202506のみを抽出することを確認。
- 本Phaseは診断専用のまま。CURRENT / STATE / Cloudへの保存処理は追加していない。

M2-5L (2026-08-21) CONTENT-BASED IMPORT 事前診断 Phase1
- 既存Data Import Hub上部に「初期データ 自動判別」を統合。既存BulkImportとの別画面・別導線は増やさない。
- CSVのファイル名ではなく内部列構成から PL_CONFIRMED / WORKER_SALES / SHIPPER_AREA を判定。
- CSV内部の日付から対象年月、内部支店コード/名称からセンターを診断。
- 複数月混在、既存CURRENT登録済み月、判別不能を事前表示。
- SKDL0001は内容パーサーで日次構造を判定し、初期一括登録対象外として表示。
- PDF/XLS/XLSX/ZIPは現段階では要追加解析と明示し、勝手に分類しない。
- Phase1は完全な読取専用。CURRENT / STATE / Cloudへの保存処理を持たない。
- 既存8 SOURCE個別取込、着地予測、BulkImport登録ロジックは変更なし。

M2-5K (2026-08-21) SKDL0001取込入口のData Import Hub統合
- データ取込Hubに「日次収支・着地予測実績 / SKDL0001 / PL_DAILY_ACTUAL」を追加。主要SOURCEは7→8種類。
- SKDL0001はHubで対象年月を選択して取込。CSV内年月が選択年月と異なる場合は保存せずエラーとする。
- 保存先は既存の正式PL_DAILY_ACTUALを継続使用。SKDL0003（月次確定）とは置換せず別SOURCEとして保持。
- 着地予測画面からSKDL0001のファイル選択・ドロップUIを撤去。着地予測は正式SOURCEの閲覧・予測に専念。
- Legacyが残る間だけ「旧日別データ 移行監査」を表示。Legacyが0件になれば監査枠自体を非表示。
- 予測計算ロジック、SKDL0001正規化ロジック、月次確定ロジックは変更なし。

M2-5J (2026-08-21) Legacy整理の正式SOURCE照合ガード追加
- 正式PL_DAILY_ACTUALとLegacyを日数・営業収益・人件費・傭車費・その他経費・粗利益の6項目で照合。
- 全一致かつ正式SOURCEがREADY・issuesなしの場合だけLegacy整理ボタンを表示。
- 削除直前にも同じ6項目を再照合し、不一致・PARTIAL・読込失敗時は削除を拒否する二重ガードを追加。
- 監査表の列見出しを分離し、Legacy/正式の日数・営業収益・照合結果を明示。
- 正式SOURCE、Legacyデータ、予測計算ロジック自体は変更なし。

M2-5I (2026-08-21) SKDL0001正式SOURCE取込0件の根本原因修正
- daily_accounting_import_bridge.jsがCSV.toRows()実行前にwindow.CSV?.toRowsを依存確認していたが、CSVはsrc/app.jsのtop-level constでwindow.CSVには公開されていないため、常にDEPENDENCY_UNAVAILABLEとなっていた。
- Bridgeは同一グローバルレキシカル環境のCSVを優先し、window.CSVは互換fallbackとしてのみ使用するよう修正。
- landing_forecast.jsは取込例外をファイル別NGログへ変換するため、全ファイル失敗時でも最終toastが「0日分」と表示されていたことを確認。
- M2-5HのCONFIG参照修正は維持。データ、科目定義、日付・金額変換、正式PL_DAILY_ACTUAL保存仕様は変更なし。

M2-5H (2026-08-19) SKDL0001正式SOURCE取込0件不具合修正
- daily_accounting_normalizer.jsのcategoryFor()がwindow.CONFIGを参照していたが、実CONFIGはsrc/app.jsのtop-level const CONFIGでありwindow.CONFIGには公開されていなかったため、全科目が未分類となり0件化していた。
- categoryFor()は同一グローバルレキシカル環境のCONFIGを優先し、window.CONFIGは互換fallbackとしてのみ使用するよう修正。
- 実ファイル SKDL0001(2).csv（CP932、3,051データ行、2026/06/01〜06/10）で、3,051行すべてが既存CONFIG科目定義へ分類可能、対象日10日を確認。
- 科目定義・金額・日付変換・正式PL_DAILY_ACTUAL保存仕様は変更なし。

M2-5G-R1 (2026-08-19) 着地予測監査UI表示先の配置修正
- daily-forecast-import-rootが既にデータ管理view内に存在していたため、M2-5G初版ではcenter.htmlに追加されず、監査UIが非表示view側へ描画される状態が残っていた。
- 既存daily-forecast-import-rootをデータ管理viewから着地予測viewへ移動。IDは1個だけを維持し、renderImportPanel()の描画先を着地予測画面へ正しく接続。
- M2-5Gで修正済みの配送分析タブhidden/CSS競合とnavigation.jsのrenderImportPanel()呼出しは維持。
- データ、予測計算、正式SOURCE/Legacy判定ロジックは変更なし。

M2-5G (2026-08-19) 着地予測UI接続漏れ・配送分析タブ誤表示修正
- center.htmlの着地予測viewにdaily-forecast-import-rootを追加し、SKDL0001取込/Legacy監査UIを実表示可能にした。
- navigation.jsで着地予測表示時にrenderImportPanel()も呼び出し、初回表示・NAV.refresh時の監査パネル描画を保証。
- version2.cssで.analysis-hub-tabs[hidden]を明示的に非表示化。display:flexがHTML hidden属性を上書きし、非配送画面にも配送分析タブが見えてクリック時に配送分析へ遷移していた問題を修正。
- landing_forecast.js内にNAV.go/route-analysis呼出しがないことを確認。誤遷移原因は着地予測ロジックではなく配送分析共通タブのhidden/CSS競合。
- 既存の正式SOURCE/Legacyデータ、予測計算ロジック、配送分析本体は変更なし。

M2-5F (2026-08-19) 未使用Legacy取込コード撤去
- M2-5Dで新規SKDL0001取込を正式PL_DAILY_ACTUALへ一本化したため、landing_forecast.jsから旧CSV解析・Legacy書込専用コードを削除。
- 削除: parseDailyText(), upsertDaily(), categoryFor(), amountOf(), toDateStr()。
- STATE.dailyRecords、legacyYMs()、recordsForYM()、Legacy監査/整理UIは既存Legacy月の閲覧・移行完了まで維持。
- forecastByWeights()/simpleForecast()はForecast Engineのfallback・比較用途があるため削除しない。
- 正式Normalized Sourceのbatch/manifest/監査履歴には変更なし。

M2-5E (2026-08-19) Legacy残存監査・安全整理
- 取込管理表で正式PL_DAILY_ACTUAL Period IndexとSTATE.dailyRecordsを月別突合。
- 「正式SOURCEのみ」「正式SOURCEあり／Legacy整理可能」「Legacyのみ／要移行」を表示。
- 重複月のLegacy整理は実行直前にloadCurrent()で正式CURRENT batchを再確認し、確認不能なら削除しない。
- Legacyのみの月は自動移行・自動削除しない。
- 整理対象はSTATE.dailyRecordsのみ。正式Normalized Sourceのbatch/manifest/監査履歴は変更しない。

M2-5D (2026-08-19) SKDL0001新規取込 Legacy dual-write停止
- landing_forecast.jsのSKDL0001新規取込をDAILY_ACCOUNTING_IMPORT_BRIDGE -> PL_DAILY_ACTUAL正式SOURCEのみに変更。
- 新規取込時のparseDailyText() / upsertDaily() / STATE.dailyRecords書込を停止。
- 新規取込時のRepository.Storage.save()および旧full-state cloud syncを停止。Normalized Source Repositoryの保存経路だけを使用する。
- Version6日別SOURCE取込基盤が利用不能な場合はLegacyへ黙ってfallbackせず取込を停止してエラー表示する。
- 既存STATE.dailyRecords、parseDailyText()、upsertDaily()は旧月閲覧・移行監査のため現段階では削除しないが、新しいLegacyデータは増加しない。
- 正式SOURCE未登録の既存Legacy月に対する閲覧・削除互換は維持。

M2-5B (2026-08-19) 着地予測 月一覧 Period Index移行
- landing_forecast.jsの月一覧取得をNormalizedSourceRepository.listPeriods('PL_DAILY_ACTUAL')優先へ移行。
- M2-5A以前の正式SOURCEは既存Legacy月を候補にbootstrapPeriods()へ渡し、manifest CURRENT確認済み月だけPeriod Indexへ登録。
- Period Index登録済み月は正式月として扱い、同月Legacyを月一覧SOURCEとして補完しない。
- 正式SOURCEが存在しない旧月だけLegacy互換として月一覧へ残す。
- Period Index利用不能時はLegacy月一覧へfallbackし、既存データ閲覧を維持。
- 月一覧取得元を画面に「正式Index / 旧データ互換」と表示。
- STATE.dailyRecords自体はまだ削除せず、取込dual-write・旧月fallback撤去条件を次工程で監査する。

M2-5A (2026-08-19) Normalized Source Period Index
- NormalizedSourceRepositoryにdocument_type別のPeriod Indexを追加。
- saveBatch()でbatch + manifest CURRENT保存成功後に対象periodをIndexへ冪等登録する。
- listPeriods(documentType)で正式SOURCEの存在年月一覧を取得可能にした。
- 既存データ移行用bootstrapPeriods(documentType,candidatePeriods)を追加。候補年月をそのまま登録せず、manifest CURRENTの存在を検証した月だけIndexへ登録する。
- Period Index更新失敗で既に保存済みの正本batch/manifestを巻き戻さない。period_index_ok=falseを返し、bootstrapで修復可能な派生Indexとして扱う。
- M2-5Aではlanding_forecastの月一覧はまだ変更しない。M2-5Bで既存月をbootstrap後、正式Period Index優先へ切り替える。

M2-4B (2026-08-19) Forecast Engine 着地予測接続
- forecast_context_loader.jsを追加。過去24か月のPL_DAILY_ACTUAL CURRENTを探索し、PL_ACTUALがCONFIRMEDの月だけを学習対象とする。
- Legacy STATE.dailyRecordsは履歴学習へ混在させない。前年同月も正式日別SOURCE + PL確定月だけを利用。
- landing_forecast.jsをForecast Engineへ接続し、revenue/labor/yosha/otherをmetric別に評価して利益を再計算。
- 履歴6か月未満はLEGACY_WEIGHTEDを継続し、十分な履歴ができるまで現行予測を維持。
- 画面に採用モデル・有効履歴月数・confidence・採用理由を表示。
- LEGACY_WEIGHTEDを既存forecastByWeights()と同じ実績日ウェイト方式へ修正し、欠損日の誤算入を防止。

M2-4A (2026-08-19) Forecast Engine 基盤
- forecast_engine.jsを新設。UI/STATE/Repositoryへ直接アクセスしない純粋計算基盤として実装。
- LEGACY_WEIGHTED / SIMPLE_RUN_RATE / HISTORICAL_PROGRESS / PRIOR_YEARの4モデルを定義。
- 過去進捗は平均ではなく中央値を採用し、単月の異常値に引っ張られにくい構造とした。
- 売上・人件費・傭車費・その他費用をmetric単位で個別予測可能にした。
- UNKNOWNを0へ変換せず、対象metricにUNKNOWNがある場合はそのモデル計算を成立させない。
- 過去確定月だけを学習/バックテスト対象にできるeligible/confirmed入力契約を定義。
- バックテストは過去月を時系列に分離し、当時以前の履歴だけでHISTORICAL_PROGRESSを計算することで未来データ混入を防止。
- MAPEを主評価指標とし、履歴6か月未満では既存固定ウェイトを暫定採用、6か月以上で実績誤差最小モデルを採用候補とする。
- M2-4Aではlanding_forecast.jsの表示・採用モデル・既存予測結果は変更しない。

M2-3 (2026-08-19) 着地予測 Read Model 正式接続
- DAILY_FORECAST_READ_MODELを追加し、PL_DAILY_ACTUALのCURRENT batchを日別 revenue/labor/yosha/other/profitへ集約。
- PL_DAILY_ACTUAL CURRENTが存在する月ではLegacyを混在・補完せず、正式SOURCEだけを使用。
- 正式SOURCE未登録の旧月だけSTATE.dailyRecordsへfallbackし、既存データ互換を維持。
- amount_known=false / null金額を0扱いせずPARTIALとして着地予測を停止。
- landing_forecast.jsを非同期Read Model読取へ移行し、競合防止render tokenを追加。
- 正式SOURCE登録済み月のLegacy削除操作を拒否し、訂正版SKDL0001再取込へ誘導。
- 予測係数・計算式そのものはM2-3では変更しない。

M2-2 (2026-08-19) SKDL0001 着地予測用SOURCE正式化
- SKDL0001を PL_DAILY_ACTUAL としてData Catalogへ追加。月次PLの正本には使用せず、着地予測・月末突合専用と明示。
- Daily Accounting Normalizer / Import Bridgeを追加し、計上日・科目・金額をNormalized Sourceへimmutable batch保存。
- NormalizedSourceRepositoryの対応SOURCEへPL_DAILY_ACTUALを追加。
- SKDL0001取込時に既存STATE.dailyRecords保存を維持しつつ、Version6 Normalized Sourceへdual-writeする移行段階とした。
- UNKNOWN金額はNormalized Source上でnull + amount_known=falseとして保持し、0へ変換しない。
- M2-2では着地予測の読取元・予測係数・月次PLロジックは変更しない。

M1-2 (2026-08-18) 月次管理状態UI
- データ確認画面の先頭に MONTHLY_MANAGEMENT_STATUS の月次サマリーを追加。
- 月次収支 / 作業者分析 / 荷主・商品・エリア分析 / 配送実績 / 便別採算を業務用語で表示。
- UI側でSOURCEや状態を再計算せず、M1-1 Read Modelの結果のみを描画。
- 速報・確定・一部未確定・未登録・エラーを区別し、経営判断メッセージと不足/要確認詳細を表示。
- 既存のデータ確認サマリー、Resolution、Canonical再構築機能は維持。

Version6 M1-1: 月次管理状態 Read Model 基盤
（2026-08-18）

【GitHub正本・基準SHA】
seven123kick-art/center-dashboard
4a24997e4b9605bfa49c957f4510484faaa556d3

【変更ファイル】
- src/core/data_catalog/monthly_management_status.js（新規）
- center.html

【目的】
CURRENT Normalized Source / Canonical Analysis Read Model / Canonical Route Ledgerから、月ごとの「経営判断に使える範囲」を自動算出する読み取り専用基盤を追加。月次PLの確定状態と、作業者・荷主・配送・便別採算の分析完成度を分離して扱う。

【主な仕様】
1. PL_ACTUAL: CONFIRMED / PRELIMINARY / PARTIAL / MISSING / ERRORを判定。SKDL0003確定とSKDL0002速報を区別。
2. WORKER_SALES: Canonical作業者Read Modelを確認し、未一致/競合があればPARTIAL。
3. SHIPPER_AREA: Canonical荷主Read ModelのissuesがあればPARTIAL。
4. DELIVERY_LIST: CURRENTがありCanonical Route Ledgerを使用できればREADY。
5. ROUTE_PAYMENT: 便ごとのprofitabilityConfirmedを使用し、全便確定でCONFIRMED、一部未確定はPARTIAL。NO_RECORD / ZERO_PAYMENT / UNKNOWNを混同しない。
6. CURRENT batchが存在する0件月とSOURCE未登録を区別。0件だからLegacyや0値で補完しない。
7. DATA_PIPELINE_STATUSはIndexedDB上の監査補助情報として添付するが、CURRENT SOURCE実体より優先して業務状態を決めない。
8. 状態はSTATE / IndexedDB / Supabaseへ重複保存せず、Read Modelとして都度算出・メモリcacheのみ。normalized-source-updated等でcache無効化。
9. current_batches / source_signatureを返し、将来の月次CLOSED時に「どのCURRENT batchに対して締めたか」を記録できる土台を用意。

【公開API】
window.MONTHLY_MANAGEMENT_STATUS.build(YYYYMM)
window.MONTHLY_MANAGEMENT_STATUS.peek(YYYYMM)
window.MONTHLY_MANAGEMENT_STATUS.invalidate(YYYYMM)

【テスト】
- node --check src/core/data_catalog/monthly_management_status.js : PASS
- VMテスト: CONFIRMED PL + 全分析READY -> overall CONFIRMED : PASS
- VMテスト: PRELIMINARY PL + ROUTE_PAYMENT未登録 -> overall PRELIMINARY / 便別採算MISSING : PASS
- VMテスト: PL未登録 -> overall INCOMPLETE : PASS
- VMテスト: 作業者未一致 + 荷主issue + 便別一部未確定 -> CONFIRMED_WITH_WARNINGS / 各PARTIAL : PASS
- CURRENT batch 0件をMISSING扱いしないこと : PASS
- center.html script読込順（Canonical Analysis -> M1-1 -> Verification Detail）: PASS

【今回未実装】
- 月次状態のUI表示
- 人によるCLOSED/承認
- 月次締めRepository
これらはM1-1のRead Model検証後に別工程で実装する。

============================================================
Version6 D4-40: 荷主分析 Canonical一本化・旧描画整理
（2026-08-18）

【GitHub正本・基準SHA】
seven123kick-art/center-dashboard
ce0c7fd6f76ac393a0e2f1dfbe768707fcd89d1e

【変更ファイル】
- src/modules/shipper.js
- src/modules/dashboard.js
- src/modules/trend.js

【目的】
荷主分析のSOURCE選択を月単位で一元化し、Canonical描画後にLegacy Datasetで再描画していた旧処理を撤去した。
Canonical READY月ではCanonicalのみ、未移行月だけLegacy fallbackを使用する。同一月で両SOURCEを混在させず、Canonical 0件もLegacyで補完しない。

【主な変更】
1. window.SHIPPER_DATA_ACCESSを追加し、resolveMonth(ym)でCanonical / Legacy fallback / NOT_LOADEDを一意に判定。
2. D4-39暫定global window.__D4_39_resolveShipperGroupsForYMを撤去。
3. 専用荷主分析をCanonical正本へ統一。Canonical描画後にgroupsOf(ds)/contractsOf(ds)で再描画する旧renderShipperラッパーを削除。
4. 旧ラッパーにあった「営業収益との差分チェック」はCanonical正本側へ移し、機能を維持。
5. ホーム上位8社、前月・前年比較、専用比較、異常検知、荷主推移をSHIPPER_DATA_ACCESSへ統一。
6. dashboard.jsの旧Legacy荷主バー描画を削除。shipper.jsのCanonical対応Hookのみが描画する構造へ整理。
7. trend.jsの旧Legacy主要荷主推移描画を削除。shipper.jsの月単位Canonical対応Hookへ統一。
8. 2026-04-29の旧荷主CSV集計・CSV.parseSKDL直接モンキーパッチ・processDataset直接モンキーパッチを削除。後段の現行V3 Hook実装が完全代替しているため二重処理を解消。

【残したLegacy】
- ds.shippers / ds.shipperGroups / ds.shipperContracts
- 現行V3のLegacy荷主集計Hook
理由：SHIPPER_AREA CURRENTが存在しない旧月のfallbackとしてSHIPPER_DATA_ACCESSから現在も参照するため。
これらはCanonical移行済み月での混在には使用しない。

【Canonical / Legacy判定】
- peek(ym)がnull/undefined: NOT_LOADED。loadMonth対象。
- READY + shipperあり: Canonical groups/contractsのみ。
- READY + shipperなし: Legacy fallback。再loadしない。
- LEGACY_FALLBACK / Canonical基盤利用不能: Legacy fallback。
- READY + groups=[]: Canonical 0件。Legacy補完なし。

【テスト】
- node --check src/modules/shipper.js : PASS
- node --check src/modules/dashboard.js : PASS
- node --check src/modules/trend.js : PASS
- Node VMで修正版shipper.js本体をロードし、以下を確認: PASS
  1) peekなし -> NOT_LOADED
  2) READY + shipper.groups=[] -> Canonical 0件維持
  3) READY + shipper=null -> Legacy fallback、再load対象外
  4) READY + shipperあり -> Canonical採用
- D4-39暫定global残存: 0件
- Canonical後段Legacy renderShipperラッパー残存: 0件
- 旧dashboard荷主バーLegacy描画: 撤去確認
- 旧trend主要荷主Legacy描画: 撤去確認
- 旧2026-04-29荷主集計モンキーパッチ: 撤去確認

【今後のLegacy撤去候補】
旧月fallbackが不要になり、全対象月にSHIPPER_AREA CURRENTが揃った段階で、ds.shippers / shipperGroups / shipperContractsおよび現行Legacy集計Hookの撤去可否を再監査する。

============================================================
Version6 D4-39: ホーム画面荷主分析 Canonical Read Model優先移行
（2026-08-18、投入前レビュー指摘の修正反映）

【投入前レビューで発見された問題と修正】
基準SHA 1610cde19d552d01d78bf795752569db78aa1a26 に対する
D4-39実装（初版）のZIPをレビューした結果、
resolveShipperGroupsForYM(ym)のNOT_LOADED判定に問題が指摘された。

既存のCANONICAL_ANALYSIS_READ_MODELS.loadMonth()は、
WORKER_SALES CURRENTだけ存在しSHIPPER_AREA CURRENTが存在しない
月でも、status:'READY', worker:非null, shipper:null を返す既存
仕様がある。初版の判定ロジックはこのケース（READYだがshipperが
null）を「READY+shipperあり」にも「LEGACY_FALLBACK」にも
一致させられず、デフォルトのNOT_LOADED分岐に落ちていた。その
結果、ホーム荷主表示で
  READY+shipperなし → NOT_LOADED → loadMonth() →
  キャッシュ済みREADY（内容は変わらない） → 再度NOT_LOADED
という再ロード・再描画ループが発生し得る状態だった。

【修正内容】
resolveShipperGroupsForYM(ym)の判定順序を、
  1. peek(ym) が null／undefined（真のキャッシュミス）
     → NOT_LOADED（loadMonth()を実行すべき唯一のケース）
  2. status==='READY' && shipperが存在
     → READY（Canonical、shipper.groupsのみ使用）
  3. status==='READY' && shipperが存在しない
     （その月にSHIPPER_AREA CURRENTが存在しない）
     → LEGACY_FALLBACK
  4. status==='LEGACY_FALLBACK'
     → LEGACY_FALLBACK
  5. Canonical基盤自体が利用不能
     → UNAVAILABLE（呼出元でLEGACY_FALLBACKと同様に扱われる）
という順序へ修正した。peek(ym)が非null（READYでも
LEGACY_FALLBACKでも）で返ってきた時点で、必ずどちらかへ即座に
確定させ、それ以上loadMonth()を呼び直さないようにした。
新しい状態（state値）や新しい集計ロジックは追加せず、既存の
判定ルールの修正のみを行った。

【追加テスト】
Node.js環境で、以下のケースを実行し期待通りの結果を確認した：
・WORKER_SALES CURRENTあり／SHIPPER_AREA CURRENTなし
  （loadMonth()結果：status:'READY', shipper:null）
  → resolveShipperGroupsForYM() = 'LEGACY_FALLBACK'
  → シミュレーションループ（最大10回反復）でloadMonth()相当の
    呼出しが0回（NOT_LOADED分岐に一度も入らないため再ロード
    自体が発生しない）であることを確認した
  → 呼出元コード（homeShipperTop8Hook内の
    `if (resolved.state === 'NOT_LOADED' && ...)`という条件）を
    確認し、LEGACY_FALLBACK時はこの分岐をスキップして直接
    groupsOf(ds)（既存Legacy荷主データ）を使用する経路に
    入ることをコードレビューで確認した
・READY+shipperあり → { state:'READY', isCanonical:true,
  groups:[Canonicalのgroups] }
・READY+shipper.groups=[] → { state:'READY', isCanonical:true,
  groups:[] }（0件でもREADY判定のまま、Legacy補完されない
  ことに変わりなし）
・LEGACY_FALLBACK → { state:'LEGACY_FALLBACK', isCanonical:false,
  groups:null }
・peek=null（NOT_LOADED） → { state:'NOT_LOADED',
  isCanonical:false, groups:null }

いずれも意図通りの結果となり、初版で確認済みだった①〜⑪の
テスト結果（前回CHANGELOG参照）にも影響がないことを確認した。

【変更ファイル（今回の修正分）】
- src/modules/shipper.js（resolveShipperGroupsForYM関数内のみ）
- CHANGELOG.txt（本セクションの追記のみ）

============================================================
以下、D4-39初版のCHANGELOG（変更なし、参考として保持）
============================================================


【GitHub正本・基準SHAの確認】
GitHub正本 seven123kick-art/center-dashboard の最新mainを取得し、
HEADのSHAが指定された基準SHA
1610cde19d552d01d78bf795752569db78aa1a26 と完全に一致することを
確認した上で作業した。

【確認済み事実の実コード検証】
src/field/shipper.js（正しくはsrc/modules/shipper.js）の
renderShipper()を実際に確認し、既にCANONICAL_ANALYSIS_READ_MODELS.
peek(ym)→READY+shipper→canonicalState.shipper.groups/contracts
という経路を持ち、未ロード時はloadMonth().then(()=>renderShipper())
で自己再描画する既存パターンがあることを確認した。
一方、ホーム画面の「荷主別売上 上位8社」（window.RENDER.
onAfterDashboard内）と「前月・前年比較ミニ表示」
（renderDashboardCompareMini、buildComparisonRows経由）は、
groupsOf(ds)/groupsFromDS(ds)というLegacy Dataset直接参照のみを
使用しており、Canonical Read Modelを一切利用していないことを
実コードで確認した。

【変更ファイル】
- src/modules/shipper.js のみ

============================================================
実装内容
============================================================

■ 1. 共通ヘルパー resolveShipperGroupsForYM(ym) の追加
対象月ymについてCANONICAL_ANALYSIS_READ_MODELS.peek(ym)を確認し、
READY+shipperが存在すればCanonicalのshipper.groupsを、
status==='LEGACY_FALLBACK'ならその旨を、まだロードされていない
（peekがnull）ならNOT_LOADEDを返す薄いルーティング関数。
新しい荷主集計アルゴリズムは含まない（既にcanonical_analysis_
read_models.jsが計算済みのgroups配列をそのまま参照するだけ）。
groupsOf(ds)自体は、ticketCountOf/renderShipperの別モンキーパッチ
/onAfterTrend等、複数の既存呼び出し元があるため、意図しない
影響を避けるため一切変更していない。

■ 2. ホーム「荷主別売上 上位8社」Hookの書き換え
window.RENDER.onAfterDashboard内の該当Hookを、名前付き関数式
（homeShipperTop8Hook）による自己再帰パターンへ書き換えた。
・resolveShipperGroupsForYMの結果がNOT_LOADEDの場合、
  「確認済みデータを読み込んでいます…」という既存デザインに
  沿ったメッセージを表示し、loadMonth()完了後に自分自身
  （同じ関数参照）を再実行する（他のHookへは一切影響しない、
  多重登録も発生しない）。
・レンダートークン（__homeShipperTop8Token）により、対象月が
  変更された後に古いloadMonth()結果が届いても、新しい月の表示を
  上書きしないようにした。
・Canonical READY月はshipper.groupsのみを使用し、0件でも
  groupsOf(ds)（Legacy）で補完しない（空表示のみ）。
・LEGACY_FALLBACK/未対応月は既存のgroupsOf(ds)をそのまま使用し、
  既存の集計・描画ロジック（差分検算表示・棒グラフ描画等）は
  一切変更していない。

■ 3. 前月・前年比較ミニ表示の書き換え
buildComparisonRows(ds)（既存、専用荷主分析画面のrenderCompare
Panelが使用）の計算ロジック本体（sort/slice/countEffect/
unitEffect分解）を、computeComparisonRowsFromGroups(curGroups,
prevGroups, lyGroups)という共通内部関数へ抽出した。
buildComparisonRows(ds)自体はこの共通関数を呼ぶだけの薄い
ラッパーへ変更したが、Legacy取得ロジック（groupsFromDS/dsByYM/
prevYM/lastYearYM）は一切変更していないため、戻り値・動作は
従来と完全に同一である（Node.js環境で新旧実装を同一入力で実行し、
JSON完全一致することを確認済み）。

新規にbuildComparisonRowsForHome(ds)を追加し、当月・前月・前年
同月それぞれについて、resolveShipperGroupsForYM経由でCanonical
優先解決を行った上で、共通のcomputeComparisonRowsFromGroups()へ
渡す構成とした。Canonicalのgroupオブジェクト（{code3,name,count,
income,unit,contracts}）は、Legacy側が期待する{key,name,count,
income,unit}という形状に変換してから渡している（keyはcode3を
採用、既存のcode4||code3優先ルールとの整合を保った）。

renderDashboardCompareMini()を、buildComparisonRowsForHome()の
結果を使うよう書き換えた。3か月のいずれかがNOT_LOADEDの場合は
「確認済みデータを読み込んでいます…」を表示し、必要な月だけを
Promise.allで並列loadMonth()した上で、完了後に自分自身を
再実行する。レンダートークン（__homeShipperCompareToken）により、
対象月変更時に古い結果で新しい月を上書きしないようにした。

【shipper.js内の複数IIFEスコープに関する対応】
shipper.jsは複数の独立したトップレベルIIFEで構成されており、
resolveShipperGroupsForYM（ホーム上位8社Hookと同じIIFE内、
10-462行目）と、前月・前年比較のロジック（別の独立したIIFE、
1281-1641行目）は直接参照し合えないスコープにあることを実装中に
発見した。ロジックを複製せず共有するため、
window.__D4_39_resolveShipperGroupsForYMという内部共有専用の
参照を用意し、比較ロジック側からこれを呼び出す形にした。

============================================================
Canonical/Legacy選択ルール
============================================================
対象月ymごとに独立して以下を判定する：
1. CANONICAL_ANALYSIS_READ_MODELS.peek(ym).status==='READY' かつ
   shipperが存在 → その月はCanonicalのshipper.groupsのみを使用。
   0件でもLegacyで補完しない。同一月でCanonical/Legacyを混ぜない。
2. peek(ym)がnull（未ロード）→ loadMonth(ym)を実行し、完了後に
   再描画する。ロード中は「確認済みデータを読み込んでいます…」
   を表示する。
3. status==='LEGACY_FALLBACK'またはCanonical基盤自体が利用不能
   → 既存のgroupsOf(ds)/groupsFromDS(ds)（Legacy Dataset直接
   参照）を使用する。

前月・前年比較ミニ表示では、当月・前月・前年同月をそれぞれ
独立してこのルールで判定するため、
「当月Canonical・前月Legacy・前年Canonical」のような組合せも
正しく成立する。ただし同一月内でCanonicalとLegacyのgroups配列を
混ぜることは一切ない。

============================================================
テスト結果
============================================================
node --check src/modules/shipper.js : PASS

Node.js環境で、resolveShipperGroupsForYM/resolveComparisonGroups
ForYM/computeComparisonRowsFromGroupsと同一のロジックを実行し、
以下を確認した：
1. Canonical READY＋荷主あり → state:'READY', isCanonical:true,
   Canonicalのgroupsがそのまま返る
2. Canonical READY＋荷主0件 → state:'READY'のまま、groups:[]
   （Legacy補完なし）
3. SHIPPER_AREA CURRENTなし（LEGACY_FALLBACK）→
   state:'LEGACY_FALLBACK', isCanonical:false
4. 初回ホーム表示でcacheなし（peekがnull）→ state:'NOT_LOADED'
   （呼出元がloadMonth()を実行する必要がある状態）
5. レンダートークンによる競合防止：コードレビューにより、
   __homeShipperTop8Token/__homeShipperCompareTokenが、
   loadMonth().then()内で「発行時のトークンと現在のトークンが
   一致する場合のみ」再描画を行う構造になっていることを確認した
   （非同期処理中に対象月が変更されトークンがインクリメントされた
   場合、古い結果は破棄される）。
6. 当月Canonical／前月Legacy → 前月データがLegacyから正しく
   取得され、countEffect等の比較計算が成立することを確認した
7. 当月・前月ともCanonical → 両月ともneedsLoad:falseとなり、
   Canonical同士で正しく比較できることを確認した
8. 前年Canonicalなし（LEGACY_FALLBACK）→ 前年だけLegacy
   fallbackし、needsLoad:falseで即座に解決されることを確認した
9. 専用荷主分析画面（renderShipper/renderComparePanel）：
   renderShipper()本体はバイト単位比較で完全一致（無変更）。
   buildComparisonRows(ds)（薄いラッパー化後）は、同一入力に
   対して旧実装とJSON完全一致の結果を返すことをNode.js環境で
   実際に新旧両実装を実行して確認した。
10. 作業者・商品・エリア・作業内容・便別採算への影響：
    src/modules/shipper.js以外のファイルは今回一切変更していない
    （diff -rqで確認済み）ため、これらの画面には影響しない。
11. コンソールエラー：全JS構文チェックでエラー0件。実ブラウザでの
    最終確認は本セッションでは実施できていない（残課題）。

============================================================
既存機能への影響
============================================================
なし。
・groupsOf(ds)/groupsFromDS(ds)/buildComparisonRows(ds)の
  Legacy取得ロジック自体は1行も変更していない。
・renderShipper()（専用荷主分析画面のCanonical接続部分）は
  バイト単位で完全一致（無変更）。
・center.html/dashboard.jsは今回一切変更していない（diff確認済み）。
・shipper.js以外の全ファイルは今回一切変更していない。
・STATE、Repository、Supabase、CSV取込仕様、dashboard.jsの
  PL/KPI計算には一切触れていない。

============================================================
懸念点・残課題
============================================================
・実ブラウザでの最終目視確認（コンソールエラー含む）は本セッション
  では実施できていない。
・shipper.js内の複数IIFEスコープをまたぐ共有手段として
  window.__D4_39_resolveShipperGroupsForYMというグローバル参照を
  追加した。これは技術的には新しいグローバル変数の追加だが、
  荷主集計ロジック自体の複製を避けるための最小限の手段であり、
  既存のwindow.CANONICAL_ANALYSIS_READ_MODELS等と同じ命名慣習
  （window直下への機能公開）に沿っている。
・Canonicalのgroupオブジェクト（code3ベース）とLegacyの
  groupsFromDS（code4||code3優先）とで、荷主のキー解決方法が
  完全に一致しない可能性がある（Canonical側にはcode4フィールドが
  存在しないため）。実データでの荷主名寄せの一致率は今回のセッション
  では検証できていない。

============================================================
最終判定
============================================================
PASS
（ホーム画面の「荷主別売上 上位8社」「前月・前年比較ミニ表示」を
Canonical Read Model優先へ移行し、専用荷主分析画面
（renderShipper/renderComparePanel）への影響がないことを実装・
検証で確認した。新しい荷主集計アルゴリズムは作成せず、既存の
CANONICAL_ANALYSIS_READ_MODELS.shipperと既存のcountEffect/
unitEffect計算ロジックを再利用した）

============================================================
Version6 D4-37: 作業内容分析 Canonical Read Model移行
（2026-08-18）

【GitHub正本・基準SHAの確認】
GitHub正本 seven123kick-art/center-dashboard の最新mainを取得し、
HEADのSHAが指定された確認時基準SHA
fbbc3620c93dfb9712b2163c85ead952b2f6532f と完全に一致することを
確認した上で作業した。

【目的】
Legacy FIELD_DATA_ACCESS.getProductRecords()を直接使用していた
作業内容分析（src/field/field_content.js）を、既存の
CANONICAL_ANALYSIS_READ_MODELS へ接続した。商品カテゴリ分析
（field_product.js）・エリア分析（field_area.js）が既に採用して
いるのと同一のパターンをそのまま踏襲した。

【変更ファイル】
- src/field/field_content.js のみ

============================================================
変更内容
============================================================

■ productRecord(ym) の書き換え
対象月ymについて、まずCANONICAL_ANALYSIS_READ_MODELS.peek(ym)を
確認する。status==='READY'かつfieldProductAreaが存在する場合は、
そのオブジェクト（__source:'CANONICAL.SHIPPER_AREA'を付与）を
そのまま返し、Legacy側の処理へは一切進まない（同一月でCanonicalと
Legacyを混ぜない、0件でもLegacyで補完しない）。
Canonical SOURCEが存在しない月のみ、従来通り
FIELD_DATA_ACCESS.getProductRecords()（またはSTATE.
productAddressData）からのLegacy取得を行う。この既存ロジック
自体は1行も変更していない。

■ render()内へのCanonical Read Modelプリロード処理の追加
field_product.js/field_area.jsと同一のパターンで、対象月の
Read Modelがまだキャッシュされていない場合（peek()がnullを返す
場合）、CANONICAL_ANALYSIS_READ_MODELS.loadMonth(ym)を非同期で
呼び出し、完了後にrenderSoon()で再描画する処理を追加した。
これがないと、初回アクセス時は常にpeek()がnullを返し、実際には
Canonicalが利用可能な月であってもLegacyへfallackし続けてしまう
（field_product.js側で既に確立されているパターンであり、新規の
挙動を発明していない）。

【表示（Canonical/Legacy判別）について】
ご指示は「商品カテゴリ分析・エリア分析と同様、可能なら画面上の
補足に表示できるようにする」というものだったが、実際に
field_product.js・field_area.jsのソースコードを確認した結果、
両画面とも__sourceフィールドをデータ内部のメタ情報として
付与しているのみで、画面上に「Canonical（確認済みSHIPPER_AREA
CURRENT SOURCE）」等のテキストを明示的に描画している箇所は
存在しないことを確認した（grepで該当テキスト0件）。
そのため、field_content.jsも同じ水準（__sourceメタ情報の付与に
とどめ、新規のUI要素・テキスト表示は追加しない）に揃えた。
これは「新しいCSSパッチは追加しない」というご指示の制約とも
合致する判断である。

============================================================
Canonical/Legacy切替条件
============================================================
1. 対象月ymについてCANONICAL_ANALYSIS_READ_MODELS.peek(ym)を確認
2. status==='READY' かつ fieldProductAreaが存在 → その月は
   Canonicalのみを使用（tickets 0件でもLegacyを足さない）
3. 上記以外（Canonical未READY・fieldProductArea不在）→ 従来の
   FIELD_DATA_ACCESS.getProductRecords()へfallback
4. 月を切り替えるたびにproductRecord(ym)が呼び直され、その月の
   peek(ym)結果に応じて1〜3が再判定される
5. render()冒頭のプリロード処理により、Canonicalがまだ
   キャッシュされていない月は非同期ロード後に自動再描画され、
   2回目以降のrender()呼び出しでは正しくCanonicalへ切り替わる

============================================================
テスト結果
============================================================
node --check src/field/field_content.js : PASS

Node.js環境でproductRecord()と同一のロジックを実行し、以下を
確認した：
1. Canonical READY＋データありのケース：__source=
   'CANONICAL.SHIPPER_AREA'、tickets 1件（Canonicalのデータが
   採用される）
2. Canonical READY＋0件のケース：__source=
   'CANONICAL.SHIPPER_AREA'のまま、tickets 0件
   （Legacy側に同じ月のデータが1件存在していても混ぜない
   ことを確認）
3. Canonicalなしのケース：__source未設定（Legacy経由）、
   tickets 1件（Legacyへ正しくfallbackする）
4. 月切替時の再判定：202606（Canonical READY）→
   'CANONICAL.SHIPPER_AREA'、202607（Canonical未READY）→
   Legacyという形で、月ごとに独立して正しく切り替わることを
   確認した
5. 既存の作業分類結果が変化しないこと：classifyWork/isCrane/
   isRecycle/isWaste/isKansen/buildRows/bars等の分類・集計関数を
   一切変更していないことをdiffで確認した。またCanonical Read
   Modelのticket構造（amount/product/productName/workDetails/
   works）が、buildRows()が既存で期待するフィールド構造と完全に
   互換であることを確認した。

============================================================
既存機能への影響
============================================================
なし。
・productRecord()の戻り値の型（{ym, tickets, ...}という構造を
  持つオブジェクトまたはnull）は変更していないため、呼出元の
  render()/buildRows()は一切変更不要だった。
・分類ロジック（大分類→中分類→小分類、クレーン、リサイクル、
  廃材、幹線除外等）は1行も変更していない。
・KPI・HTMLバー・詳細表示のHTML生成ロジックは一切変更していない。
・STATE、Repository、Supabase、SOURCE parser
  （source_normalizer.js等）、Canonical Builder
  （canonical_builder.js/canonical_analysis_read_models.js等）は
  今回一切変更していない（呼び出すだけで、モジュール自体は
  変更していない）。
・CSSは今回一切変更していない（新規CSSパッチなし）。
・field_content.js以外のファイルは今回一切変更していない。

============================================================
最終判定
============================================================
PASS

============================================================
Version6 D4-35: 便別採算 UI・出力の未確定値安全表示
（2026-08-17）

【前提・制約事項の正直な報告】
ご指示は基準コミット4dc0d37f3ac672a67de41e5155bd8844734fa2bfから
最新GitHubを取得して作業することを求めていたが、リポジトリの
owner/repo名（URL）が本セッションでは提供されておらず、また
本サンドボックス環境からGitHubへ直接pushする認証情報も持たない
ため、以下の対応とした：
・作業対象コードは、本セッションでアップロードされた
  center-dashboard-main.zip（最新コードとして提供されたもの）を
  使用した。
・実装完了後、GitHubへの反映（コミット）は本セッションでは
  実行できていない。反映作業はユーザー側で実施いただく必要が
  あり、その場合のコミットSHAは本報告に含められない。
この制約により、ご指示の完了報告項目「GitHubへ反映したコミット
SHA」は、今回のセッションでは提供できない。

【目的】
D4-34（canonical_route_ledger.js）が既に区別しているpaymentStatus
（KNOWN/ZERO_PAYMENT/NO_RECORD/SOURCE_NOT_REGISTERED/
UNKNOWN_AMOUNT）を、便別採算のUI（一覧・カード・詳細モーダル・
KPI）とExcel出力まで一貫して安全に反映した。

【変更ファイル】
- src/field/route_analysis.js のみ

============================================================
変更内容
============================================================

■ 共通表示・判定関数の新設（paymentText / isPaymentConfirmed）
既存のisProfitConfirmed/salesText/marginTextと同じパターンで、
paymentText(r)とisPaymentConfirmed(r)を新設した。

  paymentText(r)：
    r.paymentStatusが'KNOWN'         → "○○円"
    r.paymentStatusが'ZERO_PAYMENT'  → "0円（資料確認済）"
    r.paymentStatusが'NO_RECORD'     → "傭車料資料に該当なし"
    r.paymentStatusが'SOURCE_NOT_REGISTERED' → "傭車料SOURCE未登録"
    r.paymentStatusが'UNKNOWN_AMOUNT' → "傭車料金額UNKNOWN"
    r.paymentStatusが未定義（Legacy経路） → 既存表示を維持

  isPaymentConfirmed(r)：
    KNOWN/ZERO_PAYMENTならtrue、それ以外（未確定）はfalse。
    Legacy経路（paymentStatus未定義）は既存動作を維持し常にtrue。

一覧表・カード表示・詳細モーダル・KPI・Excel出力の全てが、この
2関数だけを使って傭車料の表示・確定判定を行うようにした
（場所ごとに別判定を書いていない）。

【Legacy経路の後方互換について】
D4-34のCanonical Route Ledgerが生成するrouteオブジェクトのみ
paymentStatusフィールドを持つ。D4-34以前からのLegacy経路
（src/core/ledger.js由来、LEGACY_FALLBACK時）のrouteオブジェクトは
paymentStatusを持たない。そのため、paymentText/isPaymentConfirmed
は「paymentStatus未定義＝Legacy経路」と判定し、既存の表示・確定
判定（r.paymentを常に確定額として扱う）をそのまま維持する設計と
した。これはLegacy側の内部ロジック（src/core/ledger.jsのnum()に
よる0埋め等）を今回変更しないという制約、および既存機能を壊さ
ないという原則に基づく意図的な分岐であり、D4-34のCanonical/
Legacy非混在原則（同一集計内でCanonicalとLegacyを混在させない）
自体は一切変更していない。

■ 1. 一覧表
傭車支払列を fmt(r.payment)+'円' から paymentText(r) へ置換した。

■ 2. カード表示
傭車支払欄を fmt(r.payment)+'円' から paymentText(r) へ置換した。

■ 3. 便詳細モーダル
傭車支払欄を fmt(r.payment)+'円' から paymentText(r) へ置換した。

■ 4. 上部KPI
「傭車支払」KPIカードのtotalPay計算を、rows全件の
Number(r.payment)||0合算から、isPaymentConfirmed(r)を満たす便
だけの合算へ修正した。カード下部の補足文言を「全取得便の支払
合計」（未確定便を含めて全便確定しているかのように見えた表現）
から「金額確定 N便の支払合計（未確定 M便を除く）」へ変更し、
未確定便が混在する場合にそれが分かるようにした。
「一次利益」KPIカードにも、完全連動便の便数（confirmedRows.
length）を明示する文言を追加した（従来は利益率のみの表示で、
何便分の集計かが分かりにくかった）。

■ 5. Excel出力
buildExportData()内の2箇所を修正した：
  a. 明細行の傭車支払セル：
     Number(r.payment)||0 から、isPaymentConfirmed(r)なら数値
     （KNOWN/ZERO_PAYMENT/Legacy）、未確定ならpaymentText(r)の
     状態文字列を出力するよう変更した。
  b. 傭車料状態列（新規追加）：
     paymentText(r)をそのまま出力する専用列を追加した。ZERO_
     PAYMENTの場合、傭車支払セルは数値0だが、この状態列に
     「0円（資料確認済）」と表示されるため、未確定の0（数値上は
     区別できない）と資料確認済みの0円を、この列から確実に
     判別できる。
  c. サマリー行のtotalPay：
     画面KPIと同じくisPaymentConfirmed(r)を満たす便だけの合算
     へ修正し、集計列見出しを「傭車支払（金額確定便のみ）」へ
     変更、「傭車料未確定便数」列を追加した。

■ 6. 診断メッセージ
既存の診断メッセージ（notices.push、744行目付近）に、既に
diag.routesWithoutPayment（未確定便数）を表示する仕組みが存在
することを確認した。これは既にご指示の「未確定便が混在する場合、
それが分かるようにする」という要件を満たしていたため、追加の
変更は行っていない。

============================================================
テスト結果
============================================================
Node.js環境で、paymentText()/isPaymentConfirmed()の実装を実際に
動かして以下5ケース＋関連ケースを検証した：

1. KNOWN：paymentText="15,000円"、isPaymentConfirmed=true、
   isProfitConfirmed=true（売上・傭車料とも確定、利益計算可）
2. ZERO_PAYMENT：paymentText="0円（資料確認済）"、
   isPaymentConfirmed=true、isProfitConfirmed=true
   （利益計算可、傭車料が確定0円であることが明示される）
3. NO_RECORD：paymentText="傭車料資料に該当なし"、
   isPaymentConfirmed=false、isProfitConfirmed=false
   （利益計算不可、0円扱いされていないことを確認）
4. SOURCE_NOT_REGISTERED：paymentText="傭車料SOURCE未登録"、
   isPaymentConfirmed=false、isProfitConfirmed=false
5. UNKNOWN_AMOUNT：paymentText="傭車料金額UNKNOWN"、
   isPaymentConfirmed=false、isProfitConfirmed=false

Legacy経路（paymentStatus未定義）：paymentText="8,000円"、
isPaymentConfirmed=trueとなり、既存表示が維持されることを確認した。

KPI合計テスト：上記5ケースを1つのrows配列にまとめ、
isPaymentConfirmedでフィルタした結果、金額確定便数=2件
（KNOWN+ZERO_PAYMENTのみ）、totalPay=15,000円（NO_RECORD等の
未確定分は加算されない）、未確定便数=3件と、期待通りの結果を
確認した。

Excel出力テスト：同じ5ケースで、明細行の傭車支払セルが
KNOWN/ZERO_PAYMENTのみ数値（15000/0）、NO_RECORD/
SOURCE_NOT_REGISTERED/UNKNOWN_AMOUNTは状態文字列になること、
傭車料状態列には全ケースでpaymentText()の文言がそのまま出力
されること、サマリー行のtotalPay=15,000円・
unconfirmedPaymentCount=3件となることを確認した。

============================================================
既存機能への影響
============================================================
なし。
・canonical_route_ledger.js（D4-34判定仕様ファイル）は今回
  一切変更していない（ハッシュ確認済み）。
・assets/css/features/route-analysis.cssは今回一切変更していない
  （既存の#view-route-analysisスコープ内クラスのみで表現可能な
  変更にとどめたため、新規CSS追加は不要と判断した）。
・route_analysis.js以外のファイルは今回一切変更していない。
・全JS構文チェック：エラー0件。
・Legacy経路（D4-34以前のフォールバック）の表示・確定判定は
  一切変更していない（paymentStatus未定義時の後方互換分岐で
  維持）。

============================================================
D4-34仕様の維持確認
============================================================
・Canonical/Legacy非混在原則：canonical_route_ledger.jsの
  buildMonth()自体（月全体をCanonicalまたはLegacyのどちらかへ
  切り替える判定ロジック）は今回一切変更していない。今回の
  paymentText/isPaymentConfirmedは、既に確定したrouteオブジェクト
  の表示・集計方法を変えるだけであり、CanonicalとLegacyを1つの
  集計内で混ぜる変更は行っていない。
・paymentState()の5状態判定ロジック（NO_RECORD/ZERO_PAYMENT/
  UNKNOWN_AMOUNT/SOURCE_NOT_REGISTERED/KNOWN）自体は変更して
  いない。今回はこの既存の判定結果を、UI・Excel側で正しく
  表示・集計するだけである。
・0円による補完や自社便推定は、今回の変更にも一切含まれていない。

============================================================
最終判定
============================================================
PASS
（一覧表・カード・詳細モーダル・KPI・Excel出力の全てが
paymentText()/isPaymentConfirmed()という単一の共通関数を通じて
傭車料状態を安全に表示・集計するようになった。D4-34の判定仕様・
Canonical/Legacy非混在原則には一切触れていない。GitHubへの反映
コミットSHAは、本セッションの権限・情報の制約により提供できて
いない点を残課題として明記する）

2026-08-21 M2-5M 初期データ自動判別拡張
- CSV内部の日付列を優先して対象年月を抽出。YYYY/MM/DD・YYYY-MM-DD・YYYYMMDDを対象化。
- WORKER_SALES / SHIPPER_AREA のセンター判定を内部行から補強。ファイル名は判定根拠に使用しない。
- 配達ヘッド傭車料確認 xls/xlsx を既存SheetJS基盤で内容解析し、ヘッド番号・配達日・傭車料の列構成から ROUTE_PAYMENT と判定。
- SKFL0001 PDFを既存PLAN_PDF_IMPORT.parseFileへ接続し、年度・センター・主要予算科目から PLAN_BUDGET と判定。
- 同一SOURCE・同一期間・同一センターが複数選択された場合「重複候補」を表示。
- ZIPは誤判定防止のため引き続き要追加解析。診断段階ではCURRENT/STATE/Cloudへ保存しない。
